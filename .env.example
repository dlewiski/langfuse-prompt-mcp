# Langfuse MCP Server Configuration
# Copy this file to ~/.claude/.env and fill in your values

# REQUIRED: Langfuse API Keys
# Get these from your Langfuse dashboard under Settings > API Keys
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# Langfuse Host URL
# For local Docker: http://localhost:3000
# For cloud: https://cloud.langfuse.com
LANGFUSE_HOST=http://localhost:3000

# Connection Settings
LANGFUSE_TIMEOUT=30000          # Request timeout in milliseconds (default: 30000)
LANGFUSE_MAX_RETRIES=3          # Max retry attempts (default: 3)

# CRITICAL: Flush Settings for Docker
# These ensure events are sent promptly and not lost
LANGFUSE_FLUSH_AT=1             # Send after N events (default: 1 for immediate sending)
LANGFUSE_FLUSH_INTERVAL=1000    # Flush every N milliseconds (default: 1000)

# Debug Settings
LANGFUSE_DEBUG=false            # Enable debug logging (true/false)
DEBUG=false                     # General debug mode

# Optional: Feature Flags
USE_LLM_JUDGE=true              # Use LLM for evaluation (true/false)
LLM_MODEL=claude-sonnet-4       # LLM model for evaluation
ENABLE_METRICS=false            # Enable additional metrics tracking

# Optional: Performance Tuning
MIN_IMPROVEMENT_SCORE=85        # Target score for improvements (0-100)
MAX_ITERATIONS=5                # Max improvement iterations (1-10)
PATTERN_MIN_SCORE=80            # Minimum score for pattern extraction (0-100)
CACHE_TTL=3600000              # Cache time-to-live in ms (default: 1 hour)