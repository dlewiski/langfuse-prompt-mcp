{
  "name": "langfuse-prompt-mcp",
  "version": "1.0.0",
  "description": "MCP server for Langfuse prompt management integration",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node test/test-prompt-improvement.js",
    "test:clean": "rm -rf .prompts/*.md && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "dotenv": "^16.3.1",
    "langfuse": "^3.6.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
